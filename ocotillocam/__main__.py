import asyncio
import logging
import os
import socket
import click

#wrapper?

#click commands?

def ocotillocamera(ctx, cameras, config_path, simulate, simulation_profile, verbose):

    if verbose:
        log.set_level(logging.DEBUG)
    else:
        log.set_level(logging.WARNING)

    config = None
    #parse config put in etc

async def actor(obj, host, port, actor_name):
    actor_params = {}

#async def status(obj):
#async def expose(obj, exptime, outfile, overwrite):

def main():
    ocotillocamera(obj={}, auto_envvar_prefix='OCOTILLOCAM')

if __name__ == "__main__":
    main()